providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python3 -m venv .venv",
  ".venv/bin/pip install --upgrade pip",
  ".venv/bin/pip install -r server/requirements.txt"
]

[start]
cmd = ".venv/bin/uvicorn server.license_api:app --host 0.0.0.0 --port $PORT"
