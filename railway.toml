[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn server.license_api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[environments.production.variables]
PYTHONUNBUFFERED = "1"
